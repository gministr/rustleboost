# esbuild and the Tauri CLI ship native binaries and need their install
# scripts to run; pnpm blocks build scripts unless they are allowed here.
allowBuilds:
  esbuild: true
  "@tauri-apps/cli": true
